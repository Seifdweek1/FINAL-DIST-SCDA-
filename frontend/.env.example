# Vite — copy to `.env.development.local` (gitignored) if you need overrides

# Same-origin API (recommended when SPA is served from https://localhost via nginx)
# VITE_API_BASE_URL=

# Dev proxy target for /api when using `npm run dev` (self-signed gateway)
# VITE_DEV_PROXY_TARGET=https://localhost
